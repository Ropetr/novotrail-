// Re-export produtos schemas from @erp/shared
export {
  createCategorySchema,
  updateCategorySchema,
  createProductSchema,
  updateProductSchema,
} from '@erp/shared';

// Re-export common schemas
export { paginationSchema, idParamSchema } from '@erp/shared';
